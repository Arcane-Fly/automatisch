{
  "name": "automatisch-preview",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "packages/web"
  ],
  "scripts": {
    "dev": "yarn workspace @automatisch/web dev --host 0.0.0.0 --port 3000",
    "build": "yarn workspace @automatisch/web build",
    "lint": "yarn workspace @automatisch/web lint"
  }
}
